const authRoutes = require('./authRoutes');
const blockedWebsiteRoutes = require('./blockedWebsiteRoutes');
const timeBlockRoutes = require('./timeBlockRoutes');
const focusSessionRoutes = require('./focusSessionRoutes');

module.exports = {
  authRoutes,
  blockedWebsiteRoutes,
  timeBlockRoutes,
  focusSessionRoutes
};
